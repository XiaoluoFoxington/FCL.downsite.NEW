# 线路数据归一化结构文档

## 概述

本文档描述 FCL 下载站中，经过 [`adaptDownloadData()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/index.js#L31-L40) 适配器处理后的**归一化数据结构**。归一化后的数据不再依赖 `apiVer` 字段，view 层和 controller 层可以统一消费，无需感知具体下载站（线路）的原始格式差异。

适配器的核心设计原则：
- **纯函数**：所有 adapter 都是纯转换函数，不发起网络请求
- **单一出口**：所有下载叶子节点均由 [`normalizeDownloadItem()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js#L26-L39) 统一生成
- **渐进兼容**：未登记 `apiVer` 的线路自动回退到 `plain` 兼容适配器
- **安全边界**：view 层渲染前会通过 [`isSafeNavigationUrl()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js#L131-L140) 校验下载地址

---

## 节点类型

归一化后的数据由两种节点类型组成：

| 类型 | 用途 | 渲染方式 | 判断依据 |
|------|------|----------|----------|
| **分组节点** | 选择器中的可选项（如软件名、版本号） | `<select>` 下拉框 | 包含 `children`、`items` 或 `nextUrl` 任一属性 |
| **下载叶子节点** | 最终可下载的文件 | `<table>` 表格 | 包含 `downloadUrl` 且无子节点属性 |

是否到达叶子层级由 [`isBottomLevel()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadSelectorController.js#L6-L12) 函数判断：只要数组中任一节点还有 `children`、`items` 或 `nextUrl`，就继续渲染下一级选择框；否则进入最终下载表格。

---

## 一、分组节点（Group Node）

分组节点用于构建多级选择器树。用户逐级选择后，最终到达下载叶子节点。

### 完整字段

| 键 | 类型 | 必填 | 说明 |
|----|------|------|------|
| `name` | `string` | **是** | 该分组在 UI 中的显示名称（如 `"Fold Craft Launcher"`、`"v1.2.3"`、`"线路1"` 等） |
| `default` | `boolean` | 否 | 是否为该级别的默认选中项。选择器渲染时会自动选中 `default === true` 的项；若没有默认项则选中第一个。**注意**：`random=true` 时会随机覆盖此标记 |
| `description` | `string` | 否 | 该分组的描述文本，展示在选择框下方的 `.description` 区域。默认为纯文本，除非配合 `descriptionFormat` 使用。与 `descriptionUrl` 互斥，优先使用远程 |
| `descriptionUrl` | `string` | 否 | 远程描述文本的 URL。controller 会通过 [`loadDescription()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js#L77-L80) 异步拉取并渲染到描述区域。与 `description` 互斥（优先使用远程） |
| `descriptionFormat` | `string` | 否 | 描述内容的格式。仅支持 `"html"`（走 [`createSafeContent()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js#L60-L80) DOMPurify 净化后渲染），其他值或不设置则按纯文本处理 |
| `children` | `Array<GroupNode\|LeafNode>` | 否 | 子节点列表。选择该项后，controller 递归渲染下一级。与 `nextUrl`、`items` 互斥 |
| `items` | `Array<GroupNode\|LeafNode>` | 否 | `children` 的别名，语义相同。controller 内部通过 [`normalizeInlineChildren()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadSelectorController.js#L59-L66) 统一按 `children` 处理 |
| `nextUrl` | `string` | 否 | 外部镜像 API 的请求地址。选择该项后，controller 会发起 HTTP 请求获取下级数据，再经 adapter 归一化。与 `children`/`items` 互斥 |
| `apiVersion` | `string` | 否 | 指定该节点的适配器协议名（如 `"fengyuan"`、`"Lemwood"` 等）。两种用途：<br>1. 与 `nextUrl` 配合，标记远程数据的协议<br>2. 内联 `children`/`items` 时，对子节点执行对应 adapter 归一化 |
| `sourceName` | `string` | 否 | 线路的人类可读名称，会向下传递为所有叶子节点的 `source` 字段。若不设置则回退为 `name` |
| `random` | `boolean` | 否 | 是否对子节点执行随机默认选择。设为 `true` 时，controller 会调用 [`randomlySelectDefault()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js#L42-L48) 随机标记一个子节点为 `default`。**不修改原数组**，返回新数组 |
| `filter` | `Array<string>` | 否 | 下载 URL 的正则白名单数组。匹配任一正则的 URL 才会默认显示；未匹配的会被折叠隐藏（用户可手动展开）。**继承机制**：子级未声明 `filter` 时继承父级，子级声明后覆盖父级规则 |
| `nameIsSoftware` | `boolean` | 否 | 标记该节点的 `name` 即为软件名。controller 会将其存入 `softwareName` 变量，供后续 [`loadDownloadNodes()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js#L43-L75) 调用 GitHub API 获取最新版本标签 |
| `notJoinRandom` | `boolean` | 否 | 该节点不参与随机默认选择。[`randomlySelectDefault()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js#L42-L48) 会跳过此项。可存在于分组节点和叶子节点 |

### 内联子节点归一化

当分组节点同时有 `children`/`items` 和 `apiVersion` 时，内联子节点也会经过 adapter 归一化。这一过程在 [`normalizeInlineChildren()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadSelectorController.js#L59-L66) 中完成：

```javascript
function normalizeInlineChildren(item) {
  let children = item.children || item.items || [];
  if (item.apiVersion) {
    children = adaptDownloadData(children, item.apiVersion, { source: item.sourceName || item.name });
  }
  return item.random ? randomlySelectDefault(children) : children;
}
```

这意味着本站静态配置的内联数据也可以复用已有的 adapter 逻辑。

### 典型示例

根级分组节点（由 downloadController 构造）：

```json
{
  "name": "Fold Craft Launcher",
  "nameIsSoftware": true,
  "random": true,
  "filter": ["\\.apk$", "\\.zip$"],
  "description": "Fold Craft Launcher 是一款基于 Minecraft 的启动器",
  "children": [
    {
      "name": "线路2",
      "sourceName": "线路2",
      "nextUrl": "https://fyhub.cn/api/public/v1/projects/FoldCraftLauncher/assets",
      "apiVersion": "fengyuan"
    },
    {
      "name": "线路5",
      "sourceName": "线路5",
      "nextUrl": "https://fcl.072211.xyz/api/releases?owner=FCL-Team&repo=FoldCraftLauncher&versions=20",
      "apiVersion": "Linkong",
      "notJoinRandom": true
    }
  ]
}
```

---

## 二、下载叶子节点（Download Leaf Node）

下载叶子节点是归一化管线的最终产物，直接渲染为下载表格中的一行。**所有字段由 [`normalizeDownloadItem()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js#L26-L39) 函数统一生成**，view 层只认识这些字段。

### 完整字段

| 键 | 类型 | 必填 | 说明 |
|----|------|------|------|
| `name` | `string` | **是** | 下载文件的显示名称。别名：`file_name`。通常为文件名（如 `"FCL-1.2.3-arm64-v8a.apk"`） |
| `version` | `string` | 否 | 该下载项所属的版本号。由父级分组节点传入，用于展示或后续分析扩展。在纯文件列表（如 cxsjmc）中可能为空字符串 |
| `architecture` | `string` | 否 | CPU 架构标识。别名：`arch`。优先使用线路显式提供的值（如 `"arm64-v8a"`、`"x86_64"`）。**`normalizeDownloadItem()` 仅原样拷贝此字段**；若为空，由 view 层在渲染时调用 [`inferArchitecture()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/domain/systemInfo.js#L44-L52) 从文件名/URL 推断 |
| `size` | `number\|null` | 否 | 文件大小（字节）。别名：`size_bytes`。`null` 表示未知大小，`0` 是合法值。UI 中通过 [`formatBytes()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/commonView.js#L2-L20) 格式化为人类可读形式 |
| `description` | `string` | 否 | 描述信息。别名：`unavailable_reason`。优先为正常描述文本，若下载项不可用则可能为不可用原因。在表格中显示在"描述"列 |
| `downloadUrl` | `string` | **是** | 最终下载地址。**必须是 `http://` 或 `https://` 协议的绝对 URL**，否则会被 [`isSafeNavigationUrl()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js#L131-L140) 过滤掉（在 [`selectorView.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js#L75-L83) 渲染时校验）。别名查找顺序：`downloadUrl` → `url` → `link` → `download_link` |
| `available` | `boolean` | **是** | 该下载项是否可用。默认规则：`item.available !== false`，即只要不是显式 `false` 就视为可用。若为 `false`，下载按钮会显示"暂不可用"并阻止点击跳转，但保留行信息供用户查看 |
| `source` | `string` | **是** | 所属线路的人类可读名称（如 `"线路2"`、`"线路5"`）。由 context 中的 `source` 字段传入，用于追踪下载来源 |

### 字段别名速查

`normalizeDownloadItem()` 从原始 item 中按以下优先级提取字段：

| 归一化字段 | 原始字段查找顺序 |
|-----------|-----------------|
| `name` | `item.name` → `item.file_name` → `''` |
| `architecture` | `item.architecture` → `item.arch` → `''` |
| `size` | `item.size` → `item.size_bytes` → `null` |
| `description` | `item.description` → `item.unavailable_reason` → `''` |
| `downloadUrl` | `item.downloadUrl` → `item.url` → `item.link` → `item.download_link` → `''` |

### 文件大小格式化

[`formatBytes()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/commonView.js#L2-L20) 支持两种单位制式，根据用户偏好自动选择：

- **默认（IEC 二进制）**：B / KiB / MiB / GiB / TiB（1024 进制）
- **SI 十进制**：B / KB / MB / GB / TB（1000 进制）

用户偏好存储在 `localStorage` 的 `fdn-default-format-prefix` 键中，值为 `'SI'` 时使用十进制。

### 架构推断规则

当 `architecture` 字段为空时，[`inferArchitecture()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/domain/systemInfo.js#L44-L52) 按以下优先级推断：

1. **显式字段优先**：若 `item.architecture` 非空，直接返回
2. **文件名/URL 正则匹配**：按以下顺序匹配，先匹配先返回：
   - `aarch64|arm64|armv8` → `arm64-v8a`
   - `armeabi-v7a|armv7|\barm\b` → `armeabi-v7a`
   - `x86_64|x64|amd64` → `x86_64`
   - `\bx86\b|i[36]86` → `x86`
   - `all` → `all`
3. **特殊规则**：文件名包含 `ZalithLauncher` 时返回 `all`
4. **无法识别**：返回空字符串

> **注意**：枫源适配器中有额外的特殊处理——当 `architecture === 'None'` 且文件名包含 `Zalith` 时，会手动设为 `'all'`。这是在 adapter 层完成的，不是在 `inferArchitecture()` 中。

### 典型示例

```json
{
  "name": "FCL-1.2.3-arm64-v8a.apk",
  "version": "v1.2.3",
  "architecture": "arm64-v8a",
  "size": 15728640,
  "description": "",
  "downloadUrl": "https://fyhub.cn/api/public/v1/projects/FoldCraftLauncher/assets/FCL-1.2.3-arm64-v8a.apk",
  "available": true,
  "source": "线路2"
}
```

---

## 三、数据流转全链路

```
data/mirror.json (线路配置: id, name, baseUrl, apiVer, contributeId)
       │
       ▼
data/software/{id}/detail.json (软件配置: download[{mirrorId, key, notJoinRandom}], 
       │                          randomSelectMirror, filter, description)
       ▼
downloadController.js
  ├── 根据 mirrorId 查 mirror.json 获取 apiVer、baseUrl、name
  ├── 拼接 nextUrl = baseUrl + key（通过 joinUrl 处理斜杠）
  └── 构造根级分组节点 {
        name: basic.name,
        nameIsSoftware: true,
        children: mirrorItems,
        random: detail.randomSelectMirror,
        notJoinRandom: detail.notJoinRandom,
        filter: detail.filter,
        description: detail.description
      }
       │
       ▼
downloadSelectorController.js（状态机：AbortController + 递增序号防止竞态）
  ├── 用户选择线路 → 发起 HTTP GET nextUrl
  │     │
  │     ▼
  │   downloadRepository.js → loadDownloadNodes()
  │     ├── 特殊分支：无 url 且 apiVersion='cxsjmc' → 从 GitHub API 获取最新 release
  │     ├── 正常分支：getJSON(nextUrl) → payload
  │     ├── getLatestVersion() → GitHub API（仅 Way2old/frostlynx + 指定软件名）
  │     └── adaptDownloadData(payload, apiVersion, context)
  │           │
  │           ▼
  │         adapters/download/index.js
  │           └── ADAPTERS.get(apiVer) || adaptPlain
  │                 │  以下为各 adapter 的输入格式：
  │                 ├── adaptFengyuan()   ← { data: { assets: [...] } }
  │                 ├── adaptLinkong()    ← { releases: [{ version, assets }], owner, repo }
  │                 ├── adaptLemwood()    ← { data: [...] } 或直接数组
  │                 ├── adaptFrostlynx()  ← { versions: { [version]: asset[] } }
  │                 ├── adaptCxsjmc()     ← 直接文件数组
  │                 ├── adaptWay2old()    ← { type: "directory"/"file", children }
  │                 └── adaptPlain()      ← 兼容旧格式 children/items/数组
  │                       │
  │                       ▼  所有 adapter 统一输出：
  │                     normalizeDownloadItem(item, source, version)
  │                       └── 输出归一化叶子节点
  │
  └── 渲染归一化节点到 selectorView
        ├── 分组节点 → renderSelect() → <select> 下拉框
        └── 叶子节点 → renderDownloads() → <table> 表格
```

### 状态机与竞态防护

选择器 controller 使用**双重防护机制**防止异步竞态：

1. **AbortController**：新选择立即取消旧请求，避免无用流量
2. **递增序号（requestSequence）**：即使请求忽略 abort 仍返回数据，序号校验也会阻止旧响应覆盖新状态

每次用户选择都会生成新的 `sequence`，所有异步操作完成后都通过 `isCurrent(sequence)` 校验是否仍为当前请求。

---

## 四、上游配置文件结构

### 4.1 mirror.json — 线路配置

[`data/mirror.json`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json) 是所有下载线路的全局配置表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | `number` | **是** | 线路唯一整数 ID，用于 detail.json 中引用 |
| `name` | `string` | **是** | 线路的人类可读名称（如 `"线路2"`），同时作为叶子节点的 `source` |
| `baseUrl` | `string` | **是** | API 基础 URL。可以是绝对 URL（如 `https://fyhub.cn`）或本站相对路径（如 `/data/down`） |
| `apiVer` | `string` | 否 | 适配器协议名。不设置时走 `plain` 兼容适配器 |
| `contributeId` | `Array<number>` | 否 | 贡献者 ID 列表，用于关于页展示 |

示例：
```json
{
  "id": 1,
  "name": "线路2",
  "baseUrl": "https://fyhub.cn",
  "apiVer": "fengyuan",
  "contributeId": [1]
}
```

### 4.2 detail.json — 软件配置

每个软件在 [`data/software/{id}/detail.json`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software/0/detail.json) 中定义其下载线路及其他元数据。

与下载相关的字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `download` | `Array<object>` | **是** | 下载线路列表 |
| `download[].mirrorId` | `number` | **是** | 引用 mirror.json 中的线路 ID |
| `download[].key` | `string` | **是** | 资源路径，拼接到 baseUrl 后形成完整请求地址 |
| `download[].notJoinRandom` | `boolean` | 否 | 该线路不参与随机默认选择 |
| `randomSelectMirror` | `boolean` | 否 | 是否在根级随机选择一条线路作为默认 |
| `notJoinRandom` | `boolean` | 否 | 根级节点不参与随机选择（通常用不到） |
| `filter` | `Array<string>` | 否 | 下载 URL 正则白名单，作用于所有子级 |
| `description` | `string` | 否 | 根级描述文本 |

其他字段（非下载相关）：`info`、`intro`、`releaseHistoryUrl` 等。

---

## 五、各适配器的原始格式 → 归一化映射

| 适配器 | apiVer | 原始格式（输入） | 分组依据 | 叶子字段映射 | 特殊处理 |
|--------|--------|----------|----------|-------------|----------|
| [**fengyuan**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/fengyuan.js) | `"fengyuan"` | `{ data: { assets: [{ version, file_name, size, download_path, architecture }] } }` | 按 `version` 分组，版本降序排列 | `downloadUrl` ← `new URL(download_path, baseUrl).href` | 架构为 `'None'` 且文件名含 `Zalith` 时设为 `'all'`；最高版本自动设为 default |
| [**Linkong**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/Linkong.js) | `"Linkong"` | `{ releases: [{ version, title, assets: [{ name, size, download_url }] }] }` | 每个 `release` 为一个分组 | `downloadUrl` ← `asset.download_url`（API 直接返回镜像地址） | 第一个 release 设为 default；版本名优先用 `version`，回退到 `title` |
| [**Lemwood**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/lemwood.js) | `"Lemwood"` | `{ data: [{ name/tag_name, assets: [{ name, size, downloadUrl }] }] }` 或直接数组 | 每个 release 为一个分组 | `downloadUrl` ← `asset.downloadUrl` | 兼容三种输入形态：`{data:[...]}`、直接数组、单项；第一个 release 设为 default |
| [**LemwoodLatest**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/lemwood.js) | `"LemwoodLatest"` | 同 Lemwood | 同 Lemwood，但仅保留第一个 release（`latestOnly: true`） | 同 Lemwood | 调用 `adaptLemwood(payload, context, { latestOnly: true })` |
| [**frostlynx**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/frostlynx.js) | `"frostlynx"` | `{ versions: { "v1.0": [{ name, url, size }], "v2.0": [...] } }` | 每个版本键为一个分组 | `downloadUrl` ← `item.url` | 版本名等于 `context.latestVersion` 时设为 default |
| [**cxsjmc**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/cxsjmc.js) | `"cxsjmc"` | `[{ name, url, arch/architecture, size }]`（扁平数组） | 无分组，直接叶子列表 | `downloadUrl` ← `item.url` | 仅做字段归一化，不引入分组 |
| [**Way2old**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/way2old.js) | `"Way2old"` | `{ type: "directory", name, children: [...] }` 递归树 | `type: "directory"` 为分组 | `downloadUrl` ← `item.url` | 目录名等于 `context.latestVersion` 时设为 default；未知节点类型静默忽略 |
| [**plain**](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/plain.js) | 无 / 未登记 | `[{ name, url, children }]` 或 `{ items/children: [...] }` | 保留已有 `children` 层级 | `downloadUrl` ← `item.url\|downloadUrl\|link\|download_link` | 递归处理嵌套 children；有下载 URL 的节点转为叶子，其他原样保留 |

### cxsjmc 特殊分支

cxsjmc 协议有一个特殊的无 URL 模式，在 [`loadDownloadNodes()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js#L52-L62) 中处理：

- 当 `url` 为空且 `apiVersion === 'cxsjmc'` 时，直接请求 GitHub API 获取最新 release 的 assets
- 将每个 asset 的 URL 替换为 cxsjmc 镜像地址（`https://fcl.cxsjmc.cn/FCL/{asset.name}`）
- 然后再调用 `adaptCxsjmc()` 归一化

> 注意：当前 mirror.json 中的线路11（cxsjmc）有 baseUrl 和 key，走的是正常请求路径，不触发此特殊分支。

---

## 六、GitHub API 集成

### 6.1 最新版本获取

以下协议的 `default` 标记依赖 GitHub API 获取的最新版本号：
- `Way2old`
- `frostlynx`

触发条件（[`getLatestVersion()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js#L20-L36)）：
1. `apiVersion` 是 `Way2old` 或 `frostlynx`
2. `softwareName` 在 `GITHUB_REPOSITORIES` 映射表中有对应仓库

当前映射表：
| 软件名 | GitHub 仓库 |
|--------|------------|
| Fold Craft Launcher | FCL-Team/FoldCraftLauncher |
| Zalith Launcher 2 | ZalithLauncher/ZalithLauncher2 |

获取失败时降级为 payload 自带的 `latest` 字段，若仍没有则返回 `null`。

### 6.2 cxsjmc 无 URL 模式

见上文"cxsjmc 特殊分支"。

---

## 七、辅助字段速查

以下字段存在于数据流中但不属于归一化叶子节点，仅供 controller 和 selector 中间层使用：

| 字段 | 所在层级 | 说明 |
|------|----------|------|
| `apiVersion` | 分组节点 | 用于指定 adapter 协议，controller 消费后不传递给 view |
| `nextUrl` | 分组节点 | 远程数据请求地址，controller 消费后不传递给 view |
| `sourceName` | 分组节点 | 线路显示名，向下传递为叶子节点的 `source` |
| `nameIsSoftware` | 分组节点 | 标记软件名，controller 用于 GitHub API 查询 |
| `random` | 分组节点 | 是否随机默认选择，controller 消费后不传递给 view |
| `filter` | 分组节点 | URL 正则白名单，向下继承传递给 view 用于表格行过滤 |
| `descriptionUrl` | 分组节点 | 远程描述地址，controller 异步拉取并渲染 |
| `descriptionFormat` | 分组节点 | 描述格式标记（`"html"` 或纯文本） |
| `description` | 分组节点 | 本地描述文本 |
| `notJoinRandom` | 分组/叶子节点 | 不参与随机默认选择，[`randomlySelectDefault()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js#L42-L48) 跳过 |
| `matchedArchitecture` | 外部参数 | 用户设备的 CPU 架构，由 [`detectSystemInfo()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/domain/systemInfo.js#L25-L38) 检测，view 用于绿色高亮推荐行 |
| `latestVersion` | context | 通过 GitHub API 获取的最新版本号，用于 frostlynx/Way2old 的 default 标记 |
| `baseUrl` | context | 镜像 API 的源站 URL，用于拼接相对路径的 download_path |

---

## 八、关键约束

1. **叶子节点 `downloadUrl` 必须是 `http://` 或 `https://` 绝对 URL**，否则会被 [`isSafeNavigationUrl()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js#L131-L140) 过滤掉（在 [`selectorView.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js#L75-L83) 渲染时校验），不生成下载按钮。

2. **`available: false` 的项不会被移除**，而是显示"暂不可用"按钮并阻止跳转，确保用户仍能看到该线路提供的内容。

3. **`architecture` 为空时由 [`inferArchitecture()`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/domain/systemInfo.js#L44-L52) 自动推断**（在 [`selectorView.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js#L86-L88) 渲染阶段调用，而非 `normalizeDownloadItem()` 中）。推断优先级：显式字段 > 文件名/URL 正则匹配 > 特殊规则（如 ZalithLauncher → `all`）。

4. **`filter` 未命中不会删除行**，而是用 CSS 类 [`.xf-filter-hidden`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/css/xf.css#L66-L68) 隐藏，用户可点击按钮展开。filter 具有继承机制：子级未声明时继承父级，声明后覆盖。

5. **所有 adapter 都是纯函数**，不发起网络请求。网络请求由 [`downloadRepository.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js) 统一管理。

6. **`randomlySelectDefault()` 不修改原数组**，返回新的数组副本，避免同一份镜像响应被复用时保留过期的 `default` 状态。

7. **新增线路只需**：
   1. 创建 [`js/adapters/download/`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/) 新文件（导出 `adaptXxx` 函数）
   2. 在 [`index.js` 的 `ADAPTERS` Map](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/index.js#L16-L24) 中登记 `apiVer`
   3. 在 [`data/mirror.json`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json) 中添加线路配置
   4. 在对应软件的 `detail.json` 的 `download` 数组中添加引用

   无需修改 controller 或 view。

8. **选择器状态机使用双重防护**：AbortController + 递增序号，防止快速切换线路时的竞态条件。

9. **描述加载与下级数据加载并行执行**，避免用户等待两段串行网络延迟。描述加载失败不影响下载项展示。

---

## 九、核心源文件索引

| 文件 | 职责 |
|------|------|
| [`js/adapters/download/index.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/index.js) | 适配器注册表 `ADAPTERS`、入口 `adaptDownloadData()` |
| [`js/adapters/download/common.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/common.js) | `normalizeDownloadItem()`、`randomlySelectDefault()`、`compareVersionsDescending()` |
| [`js/adapters/download/fengyuan.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/fengyuan.js) | 枫源 API 适配器 |
| [`js/adapters/download/Linkong.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/Linkong.js) | Linkong API 适配器 |
| [`js/adapters/download/lemwood.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/lemwood.js) | Lemwood / LemwoodLatest 适配器 |
| [`js/adapters/download/frostlynx.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/frostlynx.js) | frostlynx 适配器 |
| [`js/adapters/download/cxsjmc.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/cxsjmc.js) | cxsjmc 适配器 |
| [`js/adapters/download/way2old.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/way2old.js) | Way2old 适配器 |
| [`js/adapters/download/plain.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/plain.js) | 兼容 plain 适配器（未登记 apiVer 的回退） |
| [`js/controllers/downloadController.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadController.js) | 下载页入口，构造根级分组节点，整合 mirror/detail/systemInfo |
| [`js/controllers/downloadSelectorController.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadSelectorController.js) | 选择器状态机，管理请求链路与渲染调度 |
| [`js/repositories/downloadRepository.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js) | 网络请求、GitHub API、调用 adapter |
| [`js/repositories/siteRepository.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/siteRepository.js) | 本站静态数据（mirror.json、software 配置）读取 |
| [`js/views/selectorView.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js) | 渲染选择器 `<select>` 与下载表格 `<table>` |
| [`js/views/commonView.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/commonView.js) | `formatBytes()`、`renderStatus()`、`setSoftwareHeader()` 等共用工具 |
| [`js/domain/systemInfo.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/domain/systemInfo.js) | `detectSystemInfo()`、`inferArchitecture()` |
| [`js/security/content.js`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js) | `isSafeNavigationUrl()`、`createSafeContent()`、URL 重写 |
| [`data/mirror.json`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json) | 线路配置（id, name, baseUrl, apiVer, contributeId） |
| [`css/xf.css`](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/css/xf.css) | 下载页样式（filter-hidden、matched-arch 等） |
