# 添加新资源指南

本文档详细介绍如何在 FCL 下载站中添加一个新的软件资源。按照本文档的步骤操作，你可以完整地配置一个新资源，包括列表展示、详情页、下载页、介绍页和版本历史页。

---

## 目录

- [一、概述](#一概述)
- [二、数据文件总览](#二数据文件总览)
- [三、快速开始：添加新资源的 7 个步骤](#三快速开始添加新资源的-7-个步骤)
- [四、软件目录配置（software.json）](#四软件目录配置softwarejson)
- [五、软件详情配置（detail.json）](#五软件详情配置detailjson)
- [六、图标与媒体资源](#六图标与媒体资源)
- [七、下载数据配置](#七下载数据配置)
- [八、介绍文档配置](#八介绍文档配置)
- [九、版本历史配置](#九版本历史配置)
- [十、标签配置（tag.json）](#十标签配置tagjson)
- [十一、验证与测试](#十一验证与测试)
- [十二、常见问题](#十二常见问题)

---

## 一、概述

本项目采用**数据驱动**的架构，所有软件资源均通过 JSON 配置文件定义，无需修改任何 JavaScript 代码即可添加新资源。

核心设计原则：
- **数据与视图分离**：资源信息全部存储在 `data/` 目录下的 JSON 文件中
- **约定优于配置**：通过固定的目录结构和字段名自动识别资源
- **多线路适配**：支持多种下载镜像协议，通过适配器（adapter）统一数据格式

### 页面与数据的对应关系

| 页面 | 路径 | 主要数据来源 | 核心功能 |
|------|------|-------------|----------|
| 资源列表页 | `/html/list.html` | [data/software.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software.json)、[data/tag.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/tag.json) | 展示所有软件卡片，支持搜索和标签筛选 |
| 详情页 | `/html/detail.html?id={id}` | [data/software/{id}/detail.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software/0/detail.json) | 显示软件基本信息，提供下载/介绍/历史入口 |
| 下载页 | `/html/down.html?id={id}` | `detail.json` 中的 `download` 字段、[data/mirror.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json) | 多级选择器，选择线路/版本/架构后下载 |
| 介绍页 | `/html/intro.html?id={id}` | `detail.json` 中的 `intro` 字段 | 展示软件介绍文档（Markdown 或 HTML） |
| 版本历史页 | `/html/rh.html?id={id}` | `detail.json` 中的 `releaseHistoryUrl` 字段 | 从 GitHub API 拉取 Release 历史 |

---

## 二、数据文件总览

### 目录结构

```
data/
├── software.json          # 软件目录列表（所有软件的索引）
├── tag.json               # 标签字典
├── mirror.json            # 下载镜像线路配置
├── contribute.json        # 贡献者信息
├── feedback.json          # 反馈渠道
├── setting.json           # 设置配置
├── verInfo.json           # 版本信息（git hash）
├── announcement.html      # 公告内容
├── usedProj.json          # 使用的项目
│
├── software/              # 各软件的独立数据目录
│   ├── 0/                 # 软件 ID=0 的数据
│   │   ├── detail.json    # 软件详情配置
│   │   ├── icon.avif      # 软件图标（可选）
│   │   ├── intro.html     # 本地介绍文档（可选）
│   │   └── media/         # 媒体资源目录（可选）
│   │       └── img/       # 介绍页用的图片
│   ├── 1/                 # 软件 ID=1 的数据
│   ├── 2/                 # ...
│   └── ...
│
└── down/                  # 本地下载数据（线路1的数据）
    └── 0/                 # 软件 ID=0 的下载数据
        ├── index.json     # 版本索引列表
        ├── boat.json      # 特殊版本文件
        └── 1/             # 主版本号目录
            └── 3/         # 次版本号目录
                └── 1/     # 修订号目录
                    └── 9.json  # 具体版本的下载链接
```

### 关键数据文件速查表

| 文件 | 是否必须 | 说明 |
|------|---------|------|
| `data/software.json` | 是 | 软件总目录，新增软件必须在此登记 |
| `data/software/{id}/detail.json` | 是 | 软件详情配置，定义信息、下载、介绍、历史 |
| `data/software/{id}/icon.avif` | 否 | 软件图标，未设置则显示默认占位图 |
| `data/tag.json` | 视情况 | 若使用新标签需在此添加 |
| `data/mirror.json` | 视情况 | 若使用新镜像线路需在此添加 |
| `data/down/{id}/` | 视情况 | 仅本地线路（线路1）需要 |

---

## 三、快速开始：添加新资源的 7 个步骤

假设你要添加一个 ID 为 `8`、名为 "MyLauncher" 的新软件，按照以下步骤操作：

### 步骤 1：在 `data/software.json` 中登记新软件

在数组末尾添加一个新对象：

```json
{
  "id": 8,
  "name": "MyLauncher",
  "icon": "/data/software/8/icon.avif",
  "tagIds": [0, 1],
  "detailUrl": "/data/software/8/detail.json"
}
```

### 步骤 2：创建软件数据目录

```
data/software/8/
├── detail.json
└── icon.avif  (可选)
```

### 步骤 3：编写 `detail.json`

至少包含 `info`、`intro`、`download` 三个字段。

### 步骤 4：配置下载线路

根据实际情况，在 `detail.json` 的 `download` 数组中添加一个或多个下载源。

### 步骤 5：配置介绍文档

在 `detail.json` 的 `intro` 数组中添加介绍文档来源（GitHub README 或本地 HTML）。

### 步骤 6：配置版本历史

设置 `releaseHistoryUrl` 指向 GitHub Releases API。

### 步骤 7：验证测试

访问各页面确认数据正确加载。

---

## 四、软件目录配置（software.json）

### 文件位置

[data/software.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software.json)

### 数据结构

`software.json` 是一个数组，每个元素代表一个软件的基础信息。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | `number` | **是** | 软件的唯一整数 ID，必须是整数且不能重复 |
| `name` | `string` | **是** | 软件显示名称 |
| `icon` | `string` | 否 | 图标路径，建议使用绝对路径（以 `/` 开头）。若省略则列表页和详情页显示默认占位图 |
| `tagIds` | `Array<number>` | **是** | 标签 ID 数组，对应 `tag.json` 中的标签。可为空数组 `[]` |
| `detailUrl` | `string` | **是** | 详情 JSON 文件路径，建议使用绝对路径 |

### 示例

```json
[
  {
    "id": 0,
    "name": "Fold Craft Launcher",
    "icon": "/data/software/0/icon.avif",
    "tagIds": [0, 1],
    "detailUrl": "/data/software/0/detail.json"
  },
  {
    "id": 1,
    "name": "MobileGlues",
    "icon": "/data/software/1/icon.avif",
    "tagIds": [0, 2, 3],
    "detailUrl": "/data/software/1/detail.json"
  }
]
```

### 注意事项

1. **ID 必须唯一且为整数**：系统会校验 ID 的唯一性，重复 ID 会导致加载失败
2. **ID 顺序不影响展示顺序**：列表页按数组中的原始顺序展示
3. **tagIds 必须存在于 tag.json 中**：引用不存在的标签 ID 会导致加载失败
4. **detailUrl 路径正确**：路径错误会导致详情页、下载页等所有子页面无法加载

### 相关源码

- 数据加载与校验：[getSoftwareCatalog()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/siteRepository.js#L29-L41)
- 列表渲染：[renderSoftwareList()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/listView.js#L85-L116)

---

## 五、软件详情配置（detail.json）

### 文件位置

`data/software/{id}/detail.json`

示例：[data/software/0/detail.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software/0/detail.json)

### 完整字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `info` | `Array<object>` | **是** | 详情页展示的附加信息数组（开发者、官网等） |
| `intro` | `Array<object>` | **是** | 介绍页的文档面板数组 |
| `download` | `Array<object>` | **是** | 下载线路配置数组 |
| `releaseHistoryUrl` | `string` | 否 | GitHub Releases API 地址，用于版本历史页 |
| `randomSelectMirror` | `boolean` | 否 | 是否随机选择默认下载线路，默认为 `false` |

### 1. info 字段（详情页附加信息）

`info` 是一个数组，每个元素会在详情页的"基本信息"表格中新增一行。

| 子字段 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `name` | `string` | **是** | 信息项名称（表格左侧） |
| `href` | `string` | 否 | 超链接地址，有则渲染为可点击外链，无则显示纯文本 |
| `text` | `string` | 否 | 显示文本，省略则显示 `href` 本身 |

示例：

```json
"info": [
  {
    "name": "developer",
    "href": "https://github.com/MyTeam"
  },
  {
    "name": "official website",
    "href": "https://example.com",
    "text": "访问官网"
  },
  {
    "name": "开源协议",
    "text": "GPL-3.0"
  }
]
```

### 2. intro 字段（介绍文档）

`intro` 是一个数组，每个元素对应介绍页的一个可折叠面板。

| 子字段 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `title` | `string` | **是** | 面板标题 |
| `type` | `string` | **是** | 文档类型，`"md"` 表示 Markdown，`"html"` 表示 HTML |
| `url` | `string` | **是** | 文档基础 URL（目录路径或 GitHub Raw 前缀） |
| `file` | `string` | **是** | 文档文件名，与 `url` 拼接得到完整地址 |

最终文档 URL = `url + "/" + file`（自动处理首尾斜杠）。

#### 示例 1：GitHub README（推荐）

```json
"intro": [
  {
    "title": "GH-README",
    "url": "https://raw.githubusercontent.com/MyTeam/MyLauncher/refs/heads/main",
    "file": "/README.md",
    "type": "md"
  }
]
```

#### 示例 2：本地 HTML 文档

```json
"intro": [
  {
    "title": "介绍",
    "url": "/data/software/8",
    "file": "/intro.html",
    "type": "html"
  }
]
```

### 3. download 字段（下载线路）

`download` 是一个数组，每个元素代表一个下载镜像源。

| 子字段 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `mirrorId` | `number` | **是** | 镜像线路 ID，对应 `mirror.json` 中的配置 |
| `key` | `string` | **是** | 资源路径，拼接到镜像的 `baseUrl` 后面 |
| `notJoinRandom` | `boolean` | 否 | 该线路不参与随机选择，默认为 `false` |

最终请求 URL = `mirror.baseUrl + "/" + key`（自动处理首尾斜杠）。

示例：

```json
"download": [
  {
    "mirrorId": 0,
    "key": "/8/index.json",
    "notJoinRandom": true
  },
  {
    "mirrorId": 1,
    "key": "/api/public/v1/projects/MyLauncher/assets"
  }
]
```

### 4. releaseHistoryUrl 字段（版本历史）

版本历史页的 GitHub API 地址，**必须是 `https://api.github.com/repos/...` 开头的地址**。

示例：

```json
"releaseHistoryUrl": "https://api.github.com/repos/MyTeam/MyLauncher/releases"
```

> **注意**：当前版本历史页只支持 GitHub API 格式。非 GitHub 项目的版本历史需要自行开发适配器。

### 5. randomSelectMirror 字段

设为 `true` 时，下载页会在所有线路中随机选择一个作为默认选中的线路（跳过 `notJoinRandom: true` 的线路）。

```json
"randomSelectMirror": true
```

### 完整 detail.json 示例

```json
{
  "info": [
    {
      "name": "developer",
      "href": "https://github.com/MyTeam"
    },
    {
      "name": "official website",
      "href": "https://example.com"
    },
    {
      "name": "Github",
      "href": "https://github.com/MyTeam/MyLauncher/"
    }
  ],
  "intro": [
    {
      "title": "GH-README",
      "url": "https://raw.githubusercontent.com/MyTeam/MyLauncher/refs/heads/main",
      "file": "/README.md",
      "type": "md"
    },
    {
      "title": "使用教程",
      "url": "/data/software/8",
      "file": "/intro.html",
      "type": "html"
    }
  ],
  "releaseHistoryUrl": "https://api.github.com/repos/MyTeam/MyLauncher/releases",
  "download": [
    {
      "mirrorId": 0,
      "key": "/8/index.json",
      "notJoinRandom": true
    },
    {
      "mirrorId": 1,
      "key": "/api/public/v1/projects/MyLauncher/assets"
    }
  ],
  "randomSelectMirror": true
}
```

### 相关源码

- 详情数据加载：[getSoftware()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/siteRepository.js#L76-L86)
- 详情页渲染：[renderDetail()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/detailView.js#L70-L118)
- 下载页构造：[createDownloadController()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadController.js#L17-L70)

---

## 六、图标与媒体资源

### 软件图标

- **位置**：`data/software/{id}/icon.avif`
- **格式**：推荐使用 AVIF 格式（体积小），也支持 PNG、WebP 等浏览器可识别的格式
- **尺寸**：建议 96x96 或 128x128 像素的正方形
- **引用**：在 `software.json` 的 `icon` 字段中指定路径

如果未设置图标，系统会自动使用默认占位图 `/media/img/picMissing.webp`。

### 介绍页媒体资源（可选）

如果介绍文档中使用了本地图片，建议放在以下目录：

```
data/software/{id}/
└── media/
    └── img/
        ├── screenshot1.avif
        ├── screenshot2.avif
        └── ...
```

在 `intro.html` 中引用时，路径写法为 `/data/software/{id}/media/img/xxx.avif`。

参考示例：[data/software/0/media/img/](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software/0/media/img/)

---

## 七、下载数据配置

下载系统支持多种镜像协议，所有协议经过适配器统一转换为归一化格式。关于归一化数据结构的详细说明，请参考 [线路数据归一化结构文档.md](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/线路数据归一化结构文档.md)。

### 7.1 镜像线路配置（mirror.json）

在配置下载线路之前，先了解一下镜像配置。

文件位置：[data/mirror.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | `number` | **是** | 线路唯一 ID |
| `name` | `string` | **是** | 线路显示名称（如"线路1"、"线路2"） |
| `baseUrl` | `string` | **是** | 线路基础 URL，与 `detail.json` 中的 `key` 拼接 |
| `apiVer` | `string` | 否 | 线路协议版本，决定使用哪个适配器。省略则使用默认 plain 适配器 |
| `contributeId` | `Array<number>` | 否 | 贡献者 ID 数组，对应 `contribute.json` |

### 7.2 支持的协议类型

| apiVer | 协议名 | 适用场景 | 适配器文件 |
|--------|--------|---------|-----------|
| （无） | plain | 通用兼容，简单的目录结构 | [plain.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/plain.js) |
| `fengyuan` | 丰源 API | 丰源镜像站 | [fengyuan.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/fengyuan.js) |
| `Linkong` | 凌空 API | 凌空镜像站 | [Linkong.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/Linkong.js) |
| `Lemwood` | 柠枺 API | 柠枺镜像站 | [lemwood.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/lemwood.js) |
| `frostlynx` | Frostlynx | Frostlynx 镜像站 | [frostlynx.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/frostlynx.js) |
| `cxsjmc` | 创世纪 MC | 创世纪 MC 镜像站 | [cxsjmc.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/cxsjmc.js) |
| `Way2old` | Way2old | 旧版树形目录结构 | [way2old.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/way2old.js) |

### 7.3 本地线路（线路1）数据格式

本地线路（mirrorId=0，baseUrl=`/data/down`）使用最简单的目录索引格式，适合自己托管文件的场景。

#### 目录结构

```
data/down/{softwareId}/
├── index.json      # 版本列表索引
├── boat.json       # 特殊版本（可选，在 index.json 中引用）
└── 1/              # 主版本号
    └── 3/          # 次版本号
        └── 1/      # 修订号
            └── 9.json   # 具体版本的下载文件列表
```

#### index.json（版本索引）

版本列表，数组形式，每个元素代表一个可选版本。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | `string` | **是** | 版本显示名称 |
| `nextUrl` | `string` | **是** | 下一级数据文件路径（具体版本的下载列表） |
| `default` | `boolean` | 否 | 是否为默认选中版本，通常最新版本设为 `true` |
| `description` | `string` | 否 | 版本描述文本 |

示例：

```json
[
  {
    "name": "v1.0.2",
    "nextUrl": "/data/down/8/1/0/2.json",
    "default": true
  },
  {
    "name": "v1.0.1",
    "nextUrl": "/data/down/8/1/0/1.json"
  },
  {
    "name": "v1.0.0",
    "nextUrl": "/data/down/8/1/0/0.json"
  }
]
```

> **注意**：`nextUrl` 必须是绝对路径（以 `/` 开头），因为本地线路的 baseUrl 是 `/data/down`，拼接时会有特殊处理。实际上本地线路的 key 指向 index.json，index.json 内部的 nextUrl 是完整路径。

#### 版本文件（如 1/0/2.json）

具体版本的下载文件列表，数组形式，每个元素是一个下载项。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `arch` | `string` | **是** | CPU 架构，如 `arm64-v8a`、`armeabi-v7a`、`x86_64`、`x86`、`all` |
| `url` | `string` | **是** | 下载链接（完整 URL） |

示例：

```json
[
  {
    "arch": "all",
    "url": "https://example.com/files/MyLauncher-v1.0.2-all.apk"
  },
  {
    "arch": "arm64-v8a",
    "url": "https://example.com/files/MyLauncher-v1.0.2-arm64-v8a.apk"
  },
  {
    "arch": "armeabi-v7a",
    "url": "https://example.com/files/MyLauncher-v1.0.2-armeabi-v7a.apk"
  }
]
```

#### 配置 detail.json

在 `detail.json` 的 `download` 数组中添加本地线路：

```json
"download": [
  {
    "mirrorId": 0,
    "key": "/8/index.json",
    "notJoinRandom": true
  }
]
```

### 7.4 添加新的镜像线路

如果需要接入一种新的下载站，需要：

1. 在 `data/mirror.json` 中添加线路配置
2. 在 `js/adapters/download/` 中创建新的适配器文件
3. 在 `js/adapters/download/index.js` 的 `ADAPTERS` Map 中注册

详细步骤请参考 [线路数据归一化结构文档.md](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/线路数据归一化结构文档.md) 中的说明。

### 相关源码

- 下载数据仓库：[downloadRepository.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js)
- 下载选择器控制器：[downloadSelectorController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadSelectorController.js)
- 下载视图：[selectorView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js)
- 适配器注册表：[adapters/download/index.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/index.js)

---

## 八、介绍文档配置

介绍页支持两种文档格式：**Markdown** 和 **HTML**，均支持懒加载（用户展开面板时才加载内容）。

### 8.1 Markdown 文档（推荐）

最常用的方式是直接引用 GitHub 仓库的 README.md。

在 `detail.json` 中配置：

```json
"intro": [
  {
    "title": "GH-README",
    "url": "https://raw.githubusercontent.com/MyTeam/MyLauncher/refs/heads/main",
    "file": "/README.md",
    "type": "md"
  }
]
```

- `url`：GitHub Raw 的基础路径（不含文件名）
- `file`：文件名（以 `/` 开头）
- `type`：必须为 `"md"`

Markdown 内容会经过 DOMPurify 净化后渲染，确保安全。

### 8.2 HTML 文档

对于需要复杂排版或交互的介绍内容，可以使用本地 HTML 文件。

#### 步骤

1. 在 `data/software/{id}/` 下创建 `intro.html`
2. 编写 HTML 内容
3. 在 `detail.json` 中配置

#### HTML 内容要求

- 不需要完整的 HTML 文档结构（不需要 `<html>`、`<head>`、`<body>`）
- 直接写内容即可，从 `<div>` 开始
- 图片路径使用绝对路径（以 `/` 开头）
- 外部链接建议添加 `target="_blank"`

示例：

```html
<div>
  <div class="mdui-panel" mdui-panel>
    <div class="mdui-panel-item mdui-panel-item-open">
      <div class="mdui-panel-item-header mdui-ripple">
        <div>这是什么？</div>
        <i class="mdui-panel-item-arrow mdui-icon material-icons">keyboard_arrow_down</i>
      </div>
      <div class="mdui-panel-item-body mdui-typo">
        <p>MyLauncher 是一款……</p>
        <p><img src="/data/software/8/media/img/screenshot.avif" loading="lazy"></p>
      </div>
    </div>
  </div>
</div>
```

#### detail.json 配置

```json
"intro": [
  {
    "title": "介绍",
    "url": "/data/software/8",
    "file": "/intro.html",
    "type": "html"
  }
]
```

### 8.3 多个介绍面板

`intro` 是数组，可以配置多个面板，按顺序展示：

```json
"intro": [
  {
    "title": "GH-README",
    "url": "https://raw.githubusercontent.com/MyTeam/MyLauncher/refs/heads/main",
    "file": "/README.md",
    "type": "md"
  },
  {
    "title": "使用教程",
    "url": "/data/software/8",
    "file": "/tutorial.html",
    "type": "html"
  },
  {
    "title": "常见问题",
    "url": "/data/software/8",
    "file": "/faq.html",
    "type": "html"
  }
]
```

### 相关源码

- 介绍页控制器：[createIntroController()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/introController.js#L23-L63)
- 介绍页视图：[introView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/introView.js)
- 内容安全净化：[createSafeContent()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/security/content.js)

---

## 九、版本历史配置

版本历史页从 GitHub API 拉取 Release 列表并展示。

### 配置方式

在 `detail.json` 中设置 `releaseHistoryUrl`：

```json
"releaseHistoryUrl": "https://api.github.com/repos/MyTeam/MyLauncher/releases"
```

### URL 要求

1. **必须是 GitHub API 地址**：以 `https://api.github.com/repos/` 开头
2. **必须是 releases 端点**：路径以 `/releases` 结尾
3. **公开仓库**：私有仓库需要认证（当前未实现）

### 展示内容

版本历史页会展示：
- Release 标题和标签名
- 发布时间
- 发布者头像和用户名
- Release 说明（Body，Markdown 渲染）
- 预发布标记（Pre-release）

### 注意事项

- GitHub API 有请求频率限制（未认证约 60 次/小时）
- 页面会友好地展示超限错误提示
- 非 GitHub 项目暂不支持版本历史页

### 相关源码

- 版本历史控制器：[createRhController()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/rhController.js#L9-L53)
- 版本历史视图：[rhView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/rhView.js)

---

## 十、标签配置（tag.json）

### 文件位置

[data/tag.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/tag.json)

### 数据结构

标签字典，数组形式，每个元素是一个标签定义。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | `number` | **是** | 标签唯一整数 ID |
| `name` | `string` | **是** | 标签显示名称 |

示例：

```json
[
  {
    "id": 0,
    "name": "MCJE-On-Mobile"
  },
  {
    "id": 1,
    "name": "启动器"
  },
  {
    "id": 2,
    "name": "插件"
  }
]
```

### 使用方式

在 `data/software.json` 的 `tagIds` 字段中引用标签 ID：

```json
{
  "id": 8,
  "name": "MyLauncher",
  "tagIds": [0, 1],
  ...
}
```

### 标签筛选逻辑

列表页支持多标签筛选，有两种关系模式：
- **与（and）**：软件必须同时包含所有选中的标签才会显示
- **或（or）**：软件只要包含任一选中的标签就会显示

搜索词与标签之间也有三种关系模式：与、或、非。

### 相关源码

- 标签加载：[getTags()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/siteRepository.js#L44-L49)
- 标签渲染：[renderFilterTags()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/listView.js#L26-L58)
- 筛选逻辑：[applyFilters()](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/listController.js#L29-L56)

---

## 十一、验证与测试

添加完新资源后，按照以下清单逐一验证：

### 11.1 列表页验证

访问 `/html/list.html`，确认：

- [ ] 新软件卡片正常显示
- [ ] 软件名称正确
- [ ] 图标正确显示（或显示默认占位图）
- [ ] 标签正确显示
- [ ] 点击卡片能跳转到正确的页面（默认是详情页）
- [ ] 搜索功能可以搜到新软件（按名称或 ID）
- [ ] 按标签筛选可以筛选出新软件

### 11.2 详情页验证

访问 `/html/detail.html?id={id}`，确认：

- [ ] 页面标题正确
- [ ] 工具栏图标正确
- [ ] "下载资源"、"介绍文档"、"版本历史"三个按钮可点击
- [ ] 基本信息表格显示正确
- [ ] info 字段的链接可以点击跳转
- [ ] 点击下载按钮跳转到正确的下载页
- [ ] 点击介绍按钮跳转到正确的介绍页
- [ ] 点击历史按钮跳转到正确的版本历史页

### 11.3 下载页验证

访问 `/html/down.html?id={id}`，确认：

- [ ] 页面加载成功，没有报错
- [ ] 下载线路下拉框正常显示
- [ ] 能正常切换线路
- [ ] 版本/文件列表正确加载
- [ ] 下载按钮可以点击
- [ ] 下载链接正确有效
- [ ] 架构匹配高亮正确（绿色行）

### 11.4 介绍页验证

访问 `/html/intro.html?id={id}`，确认：

- [ ] 介绍面板正常显示
- [ ] 点击展开面板能正常加载内容
- [ ] Markdown 渲染正确（如果用了 md 类型）
- [ ] HTML 内容显示正确（如果用了 html 类型）
- [ ] 图片正常加载
- [ ] 链接可以点击

### 11.5 版本历史页验证

访问 `/html/rh.html?id={id}`，确认：

- [ ] Release 列表正常加载
- [ ] Release 详情可以展开
- [ ] Markdown 正文渲染正确
- [ ] 发布者信息显示正确

### 11.6 错误处理验证

- [ ] 故意配置错误的路径，确认页面显示友好的错误信息和重试按钮
- [ ] 错误信息清晰易懂，不会白屏

### 排错技巧

1. **打开浏览器开发者工具**（F12），查看 Console 中的错误信息
2. **查看 Network 面板**，确认所有 JSON 请求都返回 200
3. **检查 JSON 格式**：使用 JSON 校验工具确保 JSON 语法正确
4. **检查路径拼写**：大小写、斜杠方向等都可能导致 404
5. **检查 ID 一致性**：`software.json` 中的 ID 与目录名、`detailUrl` 路径要一致

---

## 十二、常见问题

### Q1：添加后列表页不显示新软件？

**可能原因**：
1. `software.json` 语法错误（JSON 格式不对）
2. 新增的对象没有放在数组中
3. 浏览器缓存了旧的 `software.json`

**解决方法**：
- 使用 JSON 校验工具检查格式
- 按 F5 或 Ctrl+F5 强制刷新页面
- 在开发者工具中勾选 "Disable cache"

### Q2：详情页报错"找不到 ID 为 x 的软件"？

**可能原因**：
1. `software.json` 中没有对应 ID 的条目
2. `detailUrl` 路径错误
3. `detail.json` 文件不存在

**解决方法**：
- 检查 `software.json` 中是否有该 ID
- 确认 `detailUrl` 路径与实际文件路径一致
- 检查 Network 面板中 detail.json 请求的状态码

### Q3：下载页显示"暂无下载线路"？

**可能原因**：
1. `detail.json` 中没有 `download` 字段
2. `download` 数组为空
3. `mirrorId` 引用了不存在的线路

**解决方法**：
- 在 `detail.json` 中添加 `download` 字段并确保至少有一个条目
- 检查 `mirrorId` 是否在 `mirror.json` 中存在
- 查看 Console 中的具体错误信息

### Q4：下载列表加载失败？

**可能原因**：
1. 镜像站无法访问
2. 镜像 API 返回格式与适配器不匹配
3. `key` 路径错误

**解决方法**：
- 在 Network 面板中查看请求的 URL 和响应
- 切换到其他线路确认是否是单条线路的问题
- 如果是自建线路，检查返回的 JSON 格式是否与适配器预期一致

### Q5：介绍文档加载失败？

**可能原因**：
1. GitHub Raw 访问被墙
2. `url` 或 `file` 路径错误
3. 本地 HTML 文件不存在

**解决方法**：
- 直接在浏览器中访问拼接后的完整 URL 测试
- 确认 `url` 和 `file` 拼接后是正确的地址
- 检查文件是否存在，注意大小写

### Q6：版本历史页加载失败？

**可能原因**：
1. `releaseHistoryUrl` 格式不对
2. GitHub API 访问频率超限
3. 仓库不存在或为私有

**解决方法**：
- 确认 URL 以 `https://api.github.com/repos/` 开头，以 `/releases` 结尾
- 等待一段时间后重试（GitHub API 速率限制）
- 确认仓库是公开的

### Q7：图标不显示？

**可能原因**：
1. 图标文件路径错误
2. 文件格式浏览器不支持
3. `software.json` 中没有配置 `icon` 字段

**解决方法**：
- 直接访问图标 URL 确认可以加载
- 使用常见格式（PNG、WebP、AVIF、JPG）
- 确认 `icon` 字段路径正确（建议以 `/` 开头的绝对路径）

---

## 附录：核心文件索引

### 数据文件

| 文件 | 说明 |
|------|------|
| [data/software.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software.json) | 软件目录列表 |
| [data/tag.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/tag.json) | 标签字典 |
| [data/mirror.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/mirror.json) | 镜像线路配置 |
| [data/software/0/detail.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/software/0/detail.json) | 软件详情示例（FCL） |
| [data/down/0/index.json](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/data/down/0/index.json) | 本地下载索引示例 |

### JavaScript 源码

| 文件 | 职责 |
|------|------|
| [js/repositories/siteRepository.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/siteRepository.js) | 本站静态数据仓库（软件、标签、镜像） |
| [js/repositories/downloadRepository.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/repositories/downloadRepository.js) | 下载数据仓库（请求外部镜像） |
| [js/controllers/listController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/listController.js) | 列表页控制器 |
| [js/controllers/detailController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/detailController.js) | 详情页控制器 |
| [js/controllers/downloadController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/downloadController.js) | 下载页控制器 |
| [js/controllers/introController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/introController.js) | 介绍页控制器 |
| [js/controllers/rhController.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/controllers/rhController.js) | 版本历史页控制器 |
| [js/views/listView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/listView.js) | 列表视图 |
| [js/views/detailView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/detailView.js) | 详情视图 |
| [js/views/selectorView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/selectorView.js) | 下载选择器视图 |
| [js/views/introView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/introView.js) | 介绍视图 |
| [js/views/rhView.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/views/rhView.js) | 版本历史视图 |
| [js/adapters/download/index.js](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/js/adapters/download/index.js) | 下载适配器注册表 |

### 页面模板

| 文件 | 说明 |
|------|------|
| [html/list.html](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/html/list.html) | 列表页模板 |
| [html/detail.html](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/html/detail.html) | 详情页模板 |
| [html/down.html](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/html/down.html) | 下载页模板 |
| [html/intro.html](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/html/intro.html) | 介绍页模板 |
| [html/rh.html](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/html/rh.html) | 版本历史页模板 |

### 其他文档

- [线路数据归一化结构文档.md](file:///f:/XiaoluoFoxington/Project/FCL.downsite.NEW/线路数据归一化结构文档.md) - 下载数据适配器的详细说明
